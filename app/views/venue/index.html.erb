<div class="container">
  <% form_for @venues.each do |venue| %>
  <div class="form-group">
    <a href="<%= venue_show_path %>"><%= venue.name %></a>
  </div>
   <div class="form-group">
    <%= venue.address %>
  </div>
  <% end %>
</div>
